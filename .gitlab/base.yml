.build-base:
  image: ubuntu:22.04
  before_script:
    - GRADLE_USER_HOME="$(pwd)/.gradle"
    - export GRADLE_USER_HOME
    - apt-get --quiet update --yes
    - apt-get --quiet install --yes openjdk-17-jdk python3-pip gcc-multilib libgirepository1.0-dev libadwaita-1-dev
